<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>NextScale Automation — Modern Automation Solutions</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="NextScale Automation provides modern automation solutions, implementation, and support." />
  <!-- Inter font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --purple: #8A2BE2;
      --black: #000000;
      --text: #F3F3F3;
      --muted: #C9C9C9;
      --glass: rgba(255,255,255,0.06);
      --glass-border: rgba(255,255,255,0.12);
    }

    /* Base */
    *{ box-sizing: border-box; }
    html{ scroll-behavior: smooth; }
    body{
      margin: 0;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color: var(--text);
      background:
        linear-gradient(180deg, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.85) 100%),
        /* Replace with your actual logo path (used as the main background image) */
        url("NextScale/NextScale.png");
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      line-height: 1.6;
    }
    img{ max-width: 100%; display: block; }
    a{ color: inherit; text-decoration: none; }
    .container{ width: min(1100px, 92%); margin: 0 auto; }
    section{ padding: 80px 0; }
    @media (max-width: 768px){ section{ padding: 60px 0; } }

    /* Header */
    .site-header{
      position: sticky; top: 0; z-index: 20;
      background: rgba(0,0,0,0.55);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }
    .header-inner{
      display: flex; align-items: center; justify-content: space-between;
      padding: 14px 0;
      position: relative;
    }
    .brand{
      display: inline-flex; align-items: center; gap: 10px; font-weight: 700;
    }
    .brand img{ width: 34px; height: 34px; object-fit: contain; }
    .brand span{ font-size: 16px; letter-spacing: 0.3px; }

    /* Nav (CSS-only toggle) */
    .nav-checkbox{ position: absolute; left: -9999px; }
    .nav-toggle{
      display: none; cursor: pointer;
      border: 1px solid var(--glass-border);
      background: rgba(0,0,0,0.4);
      padding: 8px 10px; border-radius: 10px;
    }
    .nav-toggle span{
      display: block; width: 20px; height: 2px; background: #EEE; margin: 4px 0; border-radius: 2px;
    }
    .nav-links{
      list-style: none; display: flex; gap: 18px; margin: 0; padding: 0;
    }
    .nav-links a{
      font-size: 14px; color: #EDEDED; padding: 8px 10px; border-radius: 8px;
      transition: background 180ms ease, color 180ms ease, transform 180ms ease;
    }
    .nav-links a:hover{
      color: #FFFFFF;
      background: rgba(138,43,226,0.18);
      transform: translateY(-1px);
    }
    @media (max-width: 860px){
      .nav-toggle{ display: inline-block; }
      nav{ position: static; }
      .nav-links{
        display: none;
        position: absolute; right: 0; top: calc(100% + 10px);
        flex-direction: column; gap: 8px;
        background: rgba(0,0,0,0.82);
        border: 1px solid var(--glass-border);
        border-radius: 12px; padding: 12px; width: min(260px, 92vw);
        backdrop-filter: blur(10px);
      }
      .nav-checkbox:checked ~ nav .nav-links{ display: flex; }
    }

    /* Typography helpers */
    .title{
      font-size: clamp(28px, 4vw, 40px);
      margin: 0 0 8px;
      background: linear-gradient(90deg, #FFFFFF 0%, #E8DAFF 40%, var(--purple) 100%);
      -webkit-background-clip: text; background-clip: text; color: transparent;
    }
    .subtitle{ color: var(--muted); margin: 6px 0 22px; font-size: 15px; }

    /* Hero */
    .hero{ padding-top: 60px; }
    .hero .lede{
      font-size: clamp(32px, 6vw, 56px);
      line-height: 1.1; margin: 0;
      background: linear-gradient(90deg, #FFFFFF 0%, #EDE5FF 35%, var(--purple) 85%);
      -webkit-background-clip: text; background-clip: text; color: transparent;
    }
    .hero p{ max-width: 760px; color: #E5E5E5; font-size: clamp(16px, 2.2vw, 18px); margin: 12px 0 20px; }
    .btn{
      display: inline-block; padding: 12px 18px; border-radius: 12px; font-weight: 600; font-size: 14px;
      background: var(--purple); color: white; transition: transform 180ms ease, filter 180ms ease, box-shadow 180ms ease;
      box-shadow: 0 10px 24px rgba(138,43,226,0.3);
    }
    .btn:hover{ transform: translateY(-2px); filter: brightness(1.05); box-shadow: 0 14px 30px rgba(138,43,226,0.38); }

    /* Cards / Glass blocks */
    .glass{
      background: var(--glass);
      border: 1px solid var(--glass-border);
      border-radius: 14px;
      backdrop-filter: blur(8px);
      padding: 20px;
    }

    /* About */
    .about p{ color: #E6E6E6; margin: 0; font-size: 15px; }

    /* Contact */
    .contact-grid{ display: grid; gap: 18px; grid-template-columns: 1.2fr; }
    .form-group{ display: grid; gap: 8px; margin-bottom: 12px; }
    label{ font-size: 13px; color: #F0F0F0; }
    .input, .textarea{
      width: 100%; color: #F7F7F7; font-size: 14px;
      background: rgba(255,255,255,0.06);
      border: 1px solid var(--glass-border);
      border-radius: 12px; padding: 12px 14px;
      transition: border-color 160ms ease, box-shadow 160ms ease, background 160ms ease;
    }
    .textarea{ min-height: 120px; resize: vertical; }
    .input:focus, .textarea:focus{
      outline: none;
      border-color: rgba(138,43,226,0.6);
      box-shadow: 0 0 0 4px rgba(138,43,226,0.18);
      background: rgba(255,255,255,0.08);
    }
    .submit{
      background: var(--purple); color: white; border: 0; cursor: pointer;
      padding: 12px 18px; border-radius: 12px; font-weight: 600; font-size: 14px;
      transition: transform 180ms ease, filter 180ms ease, box-shadow 180ms ease;
      box-shadow: 0 10px 24px rgba(138,43,226,0.3);
    }
    .submit:hover{ transform: translateY(-2px); filter: brightness(1.05); box-shadow: 0 14px 30px rgba(138,43,226,0.38); }

    /* Footer */
    footer{
      border-top: 1px solid rgba(255,255,255,0.08);
      background: rgba(0,0,0,0.55);
      backdrop-filter: blur(8px);
    }
    .footer-inner{
      padding: 20px 0;
      display: flex; align-items: center; justify-content: space-between; gap: 10px;
      flex-wrap: wrap;
      font-size: 13px; color: var(--muted);
    }

    /* Accessibility / Motion */
    .sr-only{
      position: absolute !important; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0;
    }
    @media (prefers-reduced-motion: reduce){
      *{ transition: none !important; animation: none !important; }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="site-header">
    <div class="container header-inner">
      <a class="brand" href="#home" aria-label="NextScale Automation home">
        <img src="assets/nextscale-logo.png" alt="NextScale Automation logo" />
        <span>NextScale Automation</span>
      </a>

      <!-- Mobile toggle (CSS-only) -->
      <input type="checkbox" id="nav-toggle" class="nav-checkbox" />
      <label for="nav-toggle" class="nav-toggle" aria-label="Toggle navigation">
        <span></span><span></span><span></span>
      </label>

      <nav aria-label="Primary">
        <ul class="nav-links">
          <li><a href="#home">Home</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <!-- Hero -->
    <section id="home" class="hero">
      <div class="container">
        <h1 class="lede">Elegant, reliable automation for modern operations</h1>
        <p>
          NextScale Automation helps teams design, implement, and support scalable control systems—so you can run safer, faster, and with confidence.
        </p>
        <a href="#contact" class="btn">Get in touch</a>
      </div>
    </section>

    <!-- About -->
    <section id="about">
      <div class="container">
        <h2 class="title">About</h2>
        <p class="subtitle">Automation solutions, implementation, and support</p>
        <div class="glass about">
          <p>
            NextScale Automation delivers practical automation solutions tailored to your production goals. Our team designs and implements PLC/SCADA systems, robotics, and integrated controls, and provides ongoing maintenance and support to maximize uptime and performance.
          </p>
        </div>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact">
      <div class="container">
        <h2 class="title">Contact</h2>
        <p class="subtitle">Tell us about your project—we'll get back to you shortly</p>
        <div class="contact-grid">
          <form class="glass" action="#" method="post">
            <div class="form-group">
              <label for="name">Name</label>
              <input class="input" type="text" id="name" name="name" placeholder="Your name" required />
            </div>

            <div class="form-group">
              <label for="email">Email</label>
              <input class="input" type="email" id="email" name="email" placeholder="you@company.com" required />
            </div>

            <div class="form-group">
              <label for="message">Message</label>
              <textarea class="textarea" id="message" name="message" placeholder="How can we help?" required></textarea>
            </div>

            <button type="submit" class="submit">Send Message</button>
          </form>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container footer-inner">
      <div>NextScale Automation</div>
      <div>© 2025</div>
    </div>
  </footer>
</body>
</html>
