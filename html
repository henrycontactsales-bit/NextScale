<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>NextScale Automation — Modern Automation Solutions</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="NextScale Automation provides automation solutions, implementation, and support." />
  <!-- Inter font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --purple: #8A2BE2;
      --black: #000000;
      --text: #FFFFFF;
      --muted: #CFCFCF;
      --surface: #0B0B0B;
      --border: #1a1a1a;
      --radius: 12px;
      --shadow: 0 10px 24px rgba(0,0,0,0.45);
    }

    /* Base */
    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body{
      margin: 0;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color: var(--text);
      background:
        radial-gradient(1200px 600px at 10% -10%, rgba(138,43,226,0.20), transparent 50%),
        radial-gradient(1200px 600px at 110% 10%, rgba(138,43,226,0.12), transparent 50%),
        var(--black);
      line-height: 1.6;
    }
    img { max-width: 100%; display: block; }
    a { color: inherit; text-decoration: none; }
    .container { width: min(1100px, 92%); margin: 0 auto; }
    section { padding: 80px 0; scroll-margin-top: 84px; }
    @media (max-width: 768px) { section { padding: 60px 0; } }

    /* Header */
    .site-header{
      position: sticky; top: 0; z-index: 30;
      background: rgba(0,0,0,0.65);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }
    .header-inner{
      display: grid; grid-template-columns: 1fr auto auto; gap: 12px;
      align-items: center; padding: 14px 0;
    }
    .brand{
      display: inline-flex; align-items: center; gap: 10px; font-weight: 700;
      letter-spacing: 0.2px;
    }
    .logo-mark{
      width: 34px; height: 34px; border-radius: 10px;
      background: var(--purple); color: #000;
      display: grid; place-items: center;
      font-weight: 800; font-size: 16px;
      box-shadow: 0 8px 24px rgba(138,43,226,0.35);
    }
    .brand-name{ font-size: 16px; }

    /* Navigation (CSS-only toggle) */
    .nav-check{ position: absolute; left: -9999px; }
    .nav-toggle{
      display: none; cursor: pointer;
      border: 1px solid rgba(255,255,255,0.16);
      padding: 8px 10px; border-radius: 10px;
      background: rgba(255,255,255,0.04);
    }
    .nav-toggle span{ display: block; width: 20px; height: 2px; background: #eee; margin: 4px 0; border-radius: 2px; }
    nav ul{
      display: flex; align-items: center; gap: 18px;
      list-style: none; margin: 0; padding: 0;
    }
    nav a{
      font-size: 14px; color: #EFEFEF; padding: 8px 10px; border-radius: 8px;
      transition: color 180ms ease, background 180ms ease, transform 180ms ease;
    }
    nav a:hover{ color: #fff; background: rgba(138,43,226,0.18); transform: translateY(-1px); }

    @media (max-width: 860px){
      .header-inner{ grid-template-columns: 1fr auto; }
      .nav-toggle{ display: inline-block; }
      nav{ grid-column: 1 / -1; }
      nav ul{
        display: none;
        position: relative; margin-top: 10px;
        flex-direction: column; align-items: stretch; gap: 8px;
        background: rgba(0,0,0,0.85);
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: var(--radius);
        padding: 12px;
        backdrop-filter: blur(10px);
      }
      .nav-check:checked ~ nav ul{ display: flex; }
    }

    /* Typography helpers */
    .h1{
      font-size: clamp(32px, 6vw, 56px);
      line-height: 1.08;
      margin: 0 0 10px 0;
      background: linear-gradient(90deg, #FFFFFF 0%, #EFE8FF 40%, var(--purple) 100%);
      -webkit-background-clip: text; background-clip: text; color: transparent;
      text-wrap: balance;
    }
    .h2{
      font-size: clamp(24px, 4vw, 34px);
      margin: 0 0 8px 0;
    }
    .lead{ color: #E6E6E6; font-size: clamp(16px, 2.2vw, 18px); margin: 8px 0 20px; }
    .muted{ color: var(--muted); }

    /* Buttons */
    .btn{
      display: inline-block;
      padding: 12px 18px;
      border-radius: var(--radius);
      font-weight: 600;
      font-size: 14px;
      color: #fff;
      background: var(--purple);
      box-shadow: 0 10px 24px rgba(138,43,226,0.30);
      transition: transform 180ms ease, filter 180ms ease, box-shadow 180ms ease;
    }
    .btn:hover{ transform: translateY(-2px); filter: brightness(1.05); box-shadow: 0 14px 30px rgba(138,43,226,0.40); }
    .btn:focus-visible{ outline: 2px solid rgba(138,43,226,0.65); outline-offset: 3px; }

    /* Surfaces */
    .card{
      background: rgba(255,255,255,0.04);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 20px;
      box-shadow: var(--shadow);
    }

    /* Hero */
    .hero{ padding-top: 70px; }
    .hero .actions{ display: flex; gap: 12px; flex-wrap: wrap; }

    /* About */
    .about p{ margin: 0; color: #EDEDED; }

    /* Contact */
    .contact-grid{ display: grid; gap: 18px; grid-template-columns: 1.2fr; }
    .form-group{ display: grid; gap: 8px; margin-bottom: 12px; }
    label{ font-size: 13px; color: #F3F3F3; }
    .input, .textarea{
      width: 100%; color: #F8F8F8; font-size: 14px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.14);
      border-radius: var(--radius); padding: 12px 14px;
      transition: border-color 160ms ease, box-shadow 160ms ease, background 160ms ease;
    }
    .textarea{ min-height: 120px; resize: vertical; }
    .input:focus, .textarea:focus{
      outline: none;
      border-color: rgba(138,43,226,0.6);
      box-shadow: 0 0 0 4px rgba(138,43,226,0.18);
      background: rgba(255,255,255,0.08);
    }
    .submit{
      border: 0; cursor: pointer;
      padding: 12px 18px; border-radius: var(--radius);
      background: var(--purple); color: #fff; font-weight: 600; font-size: 14px;
      box-shadow: 0 10px 24px rgba(138,43,226,0.30);
      transition: transform 180ms ease, filter 180ms ease, box-shadow 180ms ease;
    }
    .submit:hover{ transform: translateY(-2px); filter: brightness(1.05); box-shadow: 0 14px 30px rgba(138,43,226,0.40); }
    .submit:focus-visible{ outline: 2px solid rgba(138,43,226,0.65); outline-offset: 3px; }

    /* Footer */
    footer{
      border-top: 1px solid rgba(255,255,255,0.08);
      background: rgba(0,0,0,0.65);
      backdrop-filter: blur(8px);
    }
    .footer-inner{
      padding: 20px 0;
      display: flex; align-items: center; justify-content: space-between; gap: 10px; flex-wrap: wrap;
      font-size: 13px; color: var(--muted);
    }

    /* Utilities */
    .sr-only{
      position: absolute !important; width: 1px; height: 1px; padding: 0; margin: -1px;
      overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0;
    }
    @media (prefers-reduced-motion: reduce){
      *{ transition: none !important; animation: none !important; }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="site-header" id="top">
    <div class="container header-inner">
      <a class="brand" href="#home" aria-label="NextScale Automation home">
        <span class="logo-mark" aria-hidden="true">N</span>
        <span class="brand-name">NextScale Automation</span>
      </a>

      <!-- Mobile nav toggle (CSS-only) -->
      <input type="checkbox" id="nav-check" class="nav-check" />
      <label for="nav-check" class="nav-toggle" aria-label="Toggle navigation"><span></span><span></span><span></span></label>

      <nav aria-label="Primary">
        <ul>
          <li><a href="#home">Home</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <!-- Hero -->
    <section id="home" class="hero">
      <div class="container">
        <h1 class="h1">Simple, reliable automation for modern operations</h1>
        <p class="lead">
          NextScale Automation delivers practical automation solutions, from implementation to ongoing support—helping you run safer and scale with confidence.
        </p>
        <div class="actions">
          <a href="#contact" class="btn">Contact us</a>
        </div>
      </div>
    </section>

    <!-- About -->
    <section id="about">
      <div class="container">
        <h2 class="h2">About</h2>
        <p class="muted" style="margin: 6px 0 18px;">Automation solutions, implementation, and support</p>
        <div class="card about">
          <p>
            NextScale Automation specializes in designing and deploying industrial automation systems tailored to your production goals. Our team integrates PLC/SCADA controls, robotics, and data visibility—and provides dependable support to maximize uptime and performance.
          </p>
        </div>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact">
      <div class="container">
        <h2 class="h2">Contact</h2>
        <p class="muted" style="margin: 6px 0 18px;">Tell us about your project and we’ll get back to you shortly</p>
        <div class="contact-grid">
          <form class="card" action="#" method="post">
            <div class="form-group">
              <label for="name">Name</label>
              <input class="input" type="text" id="name" name="name" placeholder="Your name" required />
            </div>

            <div class="form-group">
              <label for="email">Email</label>
              <input class="input" type="email" id="email" name="email" placeholder="you@company.com" required />
            </div>

            <div class="form-group">
              <label for="message">Message</label>
              <textarea class="textarea" id="message" name="message" placeholder="How can we help?" required></textarea>
            </div>

            <button type="submit" class="submit">Send Message</button>
          </form>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container footer-inner">
      <div>NextScale Automation</div>
      <div>© 2025</div>
    </div>
  </footer>
</body>
</html>
